<configuration>
    <jmxConfigurator/>

    <shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook"/>

    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %d{yyyy-MM-dd HH:mm:ss:SSS} %thread, %level, %logger{70},  "%msg" %replace(%ex){'[\n]+', '\\n'}%nopex%n
            </Pattern>
        </layout>
    </appender>

    <appender name="EmitToGraphiteLogbackAppender"
              class="com.expedia.www.haystack.metrics.appenders.logback.EmitToGraphiteLogbackAppender">
        <host>${HAYSTACK_GRAPHITE_HOST:-monitoring-influxdb-graphite.kube-system.svc}</host>
        <port>${HAYSTACK_GRAPHITE_PORT:-2003}</port>
        <subsystem>service-graph-graph-builder</subsystem>
    </appender>

    <root level="${HAYSTACK_LOG_LEVEL}">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="EmitToGraphiteLogbackAppender" />
    </root>
</configuration>
